# Standards Service Agents

This package anchors the always-on Service Agent Layer in LeeWay-Standards.

Execution chain rule: Standards -> Integrated -> Runtime.
Projection rule: projection repos may consume core layers, but may not privately re-own them.
