# 🔌 Machine Control Protocol (MCP) Capabilities

LeeWay Standards now governs the canonical **LeeWay VS Code MCP plane**. These MCPs are not generic tool names; they are declared runtime capabilities routed through **Agent Lee Prime first**. Tools do not speak directly to the user, and no MCP bypasses governance, receipts, or validation.

## Canonical Governance Rule

> All tools are requested through Agent Lee Prime first. MCP capabilities are subordinate runtime units, never independent final speakers.

## 1. Core LeeWay Governance MCPs

| MCP | Purpose |
|---|---|
| `leeway-agent-registry` | Canonical registry loading, identity governance, and declared agent authority. |
| `leeway-validation` | Constitutional validation, attestation, evidence, and enforcement gates. |
| `leeway-health` | Runtime health checks, diagnostics, and operational status review. |
| `leeway-planner` | Structured planning, work packets, and pending-edits workflow control. |
| `leeway-memory` | Memory provenance, retrieval, write policy, and continuity management. |
| `leeway-docs-rag` | Documentation retrieval under standards governance. |

## 2. Host, Browser, and Execution MCPs

| MCP | Purpose |
|---|---|
| `leeway-desktop-commander` | Governed desktop and local-environment command mediation. |
| `leeway-playwright` | Browser-backed inspection, UI exercise, and evidence capture. |
| `leeway-scheduling` | Scheduled task governance and recurring execution control. |
| `leeway-testsprite` | Packaged validation and scenario execution support. |

## 3. Product Engineering MCPs

| MCP | Purpose |
|---|---|
| `frontend-mcp` | Frontend architecture and high-fidelity component scaffolding. |
| `backend-mcp` | Backend logic, schemas, and server-side design. |
| `design-system-mcp` | Design tokens, CSS variables, and UI consistency rules. |
| `creative-mcp` | Creative prompts and media asset synthesis. |
| `memory-mcp` | Local vector memory access patterns and retrieval integration. |
| `scheduler-mcp` | Job timing and long-running process coordination. |
| `ui-builder-mcp` | React/Vite UI generation and governed component building. |
| `qa-mcp` | Automated testing and quality validation flows. |
| `react-native-mcp` | Mobile architecture and React Native solution work. |

## 4. Optimization and Audit MCPs

| MCP | Purpose |
|---|---|
| `leeway-build-auditor-mcp` | Build-size, bundle, and dependency validation. |
| `leeway-ci-blueprint-mcp` | CI/CD blueprint generation and pipeline hardening. |
| `leeway-edge-optimizer-mcp` | Edge-runtime and low-latency optimization. |
| `leeway-full-repo-checker-mcp` | Multi-file repository audit and repair targeting. |
| `leeway-responsive-ui-mcp` | Responsive UI audit and breakpoint correction. |
| `leeway-insforge` | InsForge-assisted interface generation under LeeWay controls. |

## 5. Visual Intelligence MCP

| MCP | Purpose |
|---|---|
| `leeway-visual-intelligence-system` | SVG reconstruction, voxel reconstruction, 3D scene reconstruction, quality verification, repair loops, asset packaging, and developer-ready project integration. |

## 6. Canonical Host-Linked Agents Adjacent to the MCP Plane

These agents are not MCP servers themselves, but they are part of the same governed execution surface and should be treated as first-class LeeWay capability units:

- `fs-nav-agent`
- `mutation-agent`
- `host-exec-agent`
- `perception-agent`
- `media-forge-agent`

## Canonical Routing Notes

- `frontend-mcp`, `qa-mcp`, `ui-builder-mcp`, `design-system-mcp`, and most orchestration-friendly development flows route through `qwen2.5-coder:7b`.
- `backend-mcp` routes through `qwen2.5-coder:14b`.
- `leeway-edge-optimizer-mcp`, `leeway-build-auditor-mcp`, and `leeway-full-repo-checker-mcp` route through `deepseek-coder-v2:16b`.
- `creative-mcp` and media interpretation routes can use `llava:7b`.
- `memory-mcp` uses `nomic-embed-text` for vector retrieval.
- The visual intelligence plane remains local-first and LeeWay-governed.
